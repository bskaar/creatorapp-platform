# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Platform Stripe Keys (Get from https://dashboard.stripe.com/apikeys)
# IMPORTANT: Use test keys (pk_test_* and sk_test_*) for development
# Only use live keys (pk_live_* and sk_live_*) in production
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key
STRIPE_SECRET_KEY=sk_test_your_secret_key

# Webhook Signing Secrets (Get from https://dashboard.stripe.com/webhooks)
# Create separate webhook endpoints for platform and connect events
STRIPE_PLATFORM_WEBHOOK_SECRET=whsec_your_platform_webhook_secret
STRIPE_CONNECT_WEBHOOK_SECRET=whsec_your_connect_webhook_secret

# Email Configuration
VITE_SUPPORT_EMAIL=support@yourdomain.com
VITE_ADMIN_EMAIL=admin@yourdomain.com

# SECURITY NOTES:
# 1. NEVER commit the .env file to version control
# 2. Rotate keys immediately if accidentally exposed
# 3. Use test keys for development, live keys only in production
# 4. Set these as environment variables in your deployment platform
